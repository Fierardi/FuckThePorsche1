[build]
  functions = "netlify/functions"  # Location of your serverless functions
  publish = "main"               # Adjust this to the folder where your HTML files are

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
